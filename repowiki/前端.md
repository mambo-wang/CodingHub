## 前端概述

CodingHub 前端基于 **Vue 3.4 / TypeScript 5.4 / Vite 5.2** 构建，采用组件化开发模式，为平台提供工具展示、论坛交流、微课视频、知识库管理、留言反馈及概览统计等完整功能。前端通过 Axios 与后端 Java API（端口 8082）通信，并支持直连 RAG Python 服务实现知识库语义检索。设计系统采用双主题方案——Cyberpunk Dark 与 Glassmorphism Light，由 Pinia 状态管理驱动主题切换。

---

### 架构总览

```mermaid
graph TD
    subgraph Pages["页面层 Pages (28)"]
        P1[ForumPage]
        P2[VideoPage]
        P3[KnowledgePage]
        P4[ToolPage]
        P5[FeedbackPage]
        P6[OverviewPage]
    end

    subgraph Components["UI 组件层 Components (36)"]
        COM1[通用组件 7]
        COM2[论坛组件 7]
        COM3[视频组件 4]
        COM4[知识库组件 7]
        COM5[反馈组件 2]
    end

    subgraph Composables["组合式函数 Composables (2)"]
        C1[useInteraction]
        C2[useContentPermissions]
    end

    subgraph Stores["状态管理 Stores (3)"]
        S1[forumStore]
        S2[themeStore]
        S3[authStore]
    end

    subgraph Services["服务层 Services (9)"]
        SV1[api.ts - Axios 实例]
        SV2[forum.ts]
        SV3[video.ts]
        SV4[knowledge.ts]
        SV5[tool.ts]
        SV6[feedback.ts]
        SV7[notification.ts]
        SV8[overview.ts]
        SV9[interaction.ts]
    end

    subgraph Types["类型定义 Types (7)"]
        T[index.ts · forum.ts · video.ts<br/>knowledge.ts · feedback.ts<br/>overview.ts · tool.ts]
    end

    Pages --> Components
    Pages --> Composables
    Pages --> Stores
    Pages --> Services
    Components --> Composables
    Composables --> Services
    Composables --> Stores
    Stores --> Services
    Services --> Types
    Services -->|HTTP| BE[后端 API :8082]
    Services -.->|直连| RAG[RAG Python 服务]
```

---

### 前端分层说明

| 层级 | 目录 | 文件数 | 职责 |
|------|------|--------|------|
| L0 | `types/` + `composables/` | 9 | 类型契约与可复用逻辑（无内部依赖） |
| L1 | `services/` | 9 | 封装 HTTP 请求，统一 API 通信 |
| L2 | `stores/` | 3 | Pinia 状态管理（论坛、主题、认证） |
| L3 | `components/` | 36 | UI 组件（通用 + 论坛 + 视频 + 知识库 + 反馈） |
| L4 | `pages/` | 28 | 页面路由入口，组装下层模块 |

### 子模块文档

| 子模块 | 说明 |
|--------|------|
| [服务与状态](前端_服务与状态.md) | 服务层（9 个 API 服务）、状态管理（3 个 Pinia Store）、组合式函数（2 个）、类型定义（7 个）及 Vite 构建配置的详细设计 |
