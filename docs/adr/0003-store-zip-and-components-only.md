# 插件持久存储仅保留 zip 原件与结构摘要，不保留解压目录

早期设计为保留解压目录供详情页展示；后详情页数据源改为上传时生成的结构摘要（components 存库），解压目录失去唯一消费者。最终决定：持久存储仅保留 zip 原件（CodeBuddy HTTP source 的下载源）与库中的结构摘要；校验时临时解压、生成摘要后即删临时目录。

反直觉点：看起来"应该保留解压后的文件"——但 CodeBuddy 通过 HTTP 只能下载单文件，最终交付物必然是 zip；详情页展示由摘要承担，解压目录没有存在理由。
