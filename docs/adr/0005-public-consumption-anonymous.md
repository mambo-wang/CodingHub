# 插件市场消费端匿名公开

CodeBuddy CLI 的 `/plugin marketplace add` 只接受 URL、不带任何认证参数（官方文档无认证选项），故 `GET /marketplace.json` 与 zip 下载端点必须匿名可访问，与工具广场"下载无需认证公开"一致。鉴权仅在上传/管理侧（复用现有登录体系，上传者本人或管理员可编辑/删除、管理员可强制下架）。插件 `.mcp.json` 指向的 MCP 端点是否鉴权由插件作者自行处理，市场不介入。
