# 2026-07-23

## CodeWiki 增量更新（repowiki/）

对比基线 commit `51db395` → `6259dde`，检测到应用源码层 67 文件变更（+3571/−791）。受影响并已增量更新的模块：

- **工具广场**：Tool 新增 `logoUrl`/`downloadCount`/`favoriteCount`；热度公式改为 `view×1 + download×2 + like×3 + favorite×4 + comment×5`（Flyway V10 回填计数）。Category 新增 `logoUrl`。新增 `ImageUploadController`（`/api/v1/uploads/images`）、工具/分类 Logo 端点、工具列表 `tagId` 过滤。
- **知识库与RAG**：RAG 重构为 `rag/api/app.py` + `rag/core/*`（profiler 自适应选策略、validator 5 条质量规则、chunker 支持 protected_spans、vector_store 混合检索 ANN+FTS+RRF、context_header）。新增分块预览端点、集合配置代理 `/api/v1/knowledge/{id}/config`。
- **MCP服务**：工具总数 18→20（新增 `h3_coding_hub_kb_get_config`、`h3_coding_hub_kb_configure`，知识库组 7→9）；`tool_search` 新增 `tag` 过滤。注意：后端代码日志字符串仍写死 "18 tools"（遗留未更新），文档按实际数量 20 记录。
- **用户与认证**：SecurityConfig 放行 `GET /api/v1/uploads/images/**`，上传需登录。
- **前端应用**：新增 `LogoUploader`、`ChunkingPreviewPanel`/`ChunkCard`/`StrategyBadge` 组件、`CategoryManagePage`(`/admin/categories`) 路由、knowledge service 的 `configureCollection`/`previewChunking`。

overview.md 中 2 处 "18 个工具" 已改为 20。`lint_wiki(stale_refs)` 健康分 100，无删除文件。已 `close_session` 触发索引重建。

注意：命令模板里的 `repo_path=/Users/kirito/repos/CodeWiki-CN` 是生成器仓库路径笔误，实际分析目标为当前工作区 `/Users/kirito/repos/CodingHub`。

## MCP Server 代码修正

`McpSdkServerConfig.java` 中 5 处写死的 "18 个工具" 已改为实际数 20（类 Javadoc、SERVER_INSTRUCTIONS、两处启动日志、registerAllTools 注释），并在 SERVER_INSTRUCTIONS 知识库能力描述中补充 "RAG 分块配置读取/更新"。经统计 registerTool 调用共 20 个，与文档一致。未提交，待人工确认。
