# 2026-07-23

## CodeWiki 增量更新（repowiki/）

对比基线 commit `51db395` → `6259dde`，检测到应用源码层 67 文件变更（+3571/−791）。受影响并已增量更新的模块：

- **工具广场**：Tool 新增 `logoUrl`/`downloadCount`/`favoriteCount`；热度公式改为 `view×1 + download×2 + like×3 + favorite×4 + comment×5`（Flyway V10 回填计数）。Category 新增 `logoUrl`。新增 `ImageUploadController`（`/api/v1/uploads/images`）、工具/分类 Logo 端点、工具列表 `tagId` 过滤。
- **知识库与RAG**：RAG 重构为 `rag/api/app.py` + `rag/core/*`（profiler 自适应选策略、validator 5 条质量规则、chunker 支持 protected_spans、vector_store 混合检索 ANN+FTS+RRF、context_header）。新增分块预览端点、集合配置代理 `/api/v1/knowledge/{id}/config`。
- **MCP服务**：工具总数 18→20（新增 `h3_coding_hub_kb_get_config`、`h3_coding_hub_kb_configure`，知识库组 7→9）；`tool_search` 新增 `tag` 过滤。注意：后端代码日志字符串仍写死 "18 tools"（遗留未更新），文档按实际数量 20 记录。
- **用户与认证**：SecurityConfig 放行 `GET /api/v1/uploads/images/**`，上传需登录。
- **前端应用**：新增 `LogoUploader`、`ChunkingPreviewPanel`/`ChunkCard`/`StrategyBadge` 组件、`CategoryManagePage`(`/admin/categories`) 路由、knowledge service 的 `configureCollection`/`previewChunking`。

overview.md 中 2 处 "18 个工具" 已改为 20。`lint_wiki(stale_refs)` 健康分 100，无删除文件。已 `close_session` 触发索引重建。

注意：命令模板里的 `repo_path=/Users/kirito/repos/CodeWiki-CN` 是生成器仓库路径笔误，实际分析目标为当前工作区 `/Users/kirito/repos/CodingHub`。

## RAG 默认嵌入模型修正（中文友好）

`rag/core/embeddings.py` 默认模型从英文的 `sentence-transformers/all-MiniLM-L6-v2`（384维，中文召回差）改为 `Qwen/Qwen3-Embedding-0.6B`（1024维，中英文双语）。原因：整个项目（README_CN.md、deploy 脚本、encode_query 的 `"query: "` 前缀、async_engine 注释）本就按 Qwen3-Embedding 设计，旧默认值是没跟上设计的 bug。同时移除未被使用的 `DEFAULT_DIMENSION=384` 常量。

注意：换模型后维度 384→1024，旧的 zvec collection 会在下次启动自动 rmtree 重建（vector_store.py:107-112），但 `rag/data/documents.db`（SQLite 元数据）不会自动清理，需手动删除整个 `rag/data/` 后重新 ingest，否则元数据与向量不一致。首次运行会下载 ~1.2GB 模型（走 hf-mirror.com）。若改用纯 CPU 轻量中文模型（如 `BAAI/bge-small-zh-v1.5`）需同步修改 encode_query 的 `"query: "` 前缀（BGE 用 "Represent this sentence for searching relevant passages: "）。未提交，待人工确认。

## RAG 下载超时根因修复（HF_ENDPOINT 未走镜像）

报错 `Connection to huggingface.co timed out` 的真正根因有两点：
1. `huggingface_hub` 在 import 时把 `HF_ENDPOINT` 缓存为模块级常量（`constants.HF_ENDPOINT = os.getenv(...)`），之后改 `os.environ` 无效。原代码镜像设置写在 `from sentence_transformers import SentenceTransformer` **之后**，此时常量早已固定成官方地址。
2. 用户运行环境的 `HF_ENDPOINT` 被设成了官方 `huggingface.co`（非空），上一轮的 `if not os.environ.get("HF_ENDPOINT")` 条件为 False，没覆盖。

最终修复：在 `import sentence_transformers` **之前**设置镜像（官方地址也强制替换为 `https://hf-mirror.com`），import **之后**刷新 `huggingface_hub.constants.HF_ENDPOINT` 常量，并强制 `TRANSFORMERS_OFFLINE/HF_HUB_OFFLINE=0`。三处（embeddings.py、reranker.py、server.py 入口）同步修复，server.py 在任意 huggingface import 前设好作为第一道防线。注意：跑 RAG 是直接 `python server.py` 而非 deploy 脚本（deploy 用本地 models/ + 离线），所以需联网经镜像下载。未提交，待人工确认。

## MCP Server 代码修正

`McpSdkServerConfig.java` 中 5 处写死的 "18 个工具" 已改为实际数 20（类 Javadoc、SERVER_INSTRUCTIONS、两处启动日志、registerAllTools 注释），并在 SERVER_INSTRUCTIONS 知识库能力描述中补充 "RAG 分块配置读取/更新"。经统计 registerTool 调用共 20 个，与文档一致。未提交，待人工确认。

## RAG transformers 版本约束与环境修复

Qwen3-Embedding 的 `qwen3` 架构需要 `transformers>=4.51`，但原 base 环境是 4.49.0 导致报 "model type qwen3 but Transformers does not recognize this architecture"。坑点：
1. 直接 `pip install --upgrade transformers` 会升到 5.x，与项目 `sentence-transformers 4.1.0`（`transformers<5.0`）冲突，并破坏 `smolagents`（`huggingface-hub<1.0`）；且 conda base（`/Users/kirito/miniconda3`）site-packages 无写权限会 `Permission denied`。
2. 正确做法：`sudo python -m pip install "transformers>=4.51,<5.0" -i https://pypi.tuna.tsinghua.edu.cn/simple`，最终装到 `transformers 4.57.6` + `huggingface-hub 0.36.2`（<1.0），与 sbert/smolagents 全部兼容。

结论：RAG 环境要求 `transformers>=4.51,<5.0` 且 `huggingface-hub<1.0`，升级 conda base 需加 `sudo`。pip 警告里的 `~afetensors/~etuptools/~egex` 是之前中断遗留的损坏包目录，可手动 `rm -rf` 清理。每次改完环境/代码都要**重启 RAG 进程**才会生效。

## RAG 异步死锁修复（uvloop + search 阻塞事件循环）

RAG 服务（uvicorn 默认用 uvloop）有两个致命问题，导致 ingest 卡在 CHUNKING、首次检索 HTTP=000 超时：
1. **uvloop 死锁**：async_engine 用 `asyncio.to_thread` 跑阻塞 C 扩展（zvec 的 `create_and_open`、sentence-transformers 模型加载），在 uvloop 下死锁（进程 CPU ~2% 假死，日志停在 `Loading embedding model` 后无进展）。修复：`uvicorn.run(app, host, port, loop="asyncio")` 禁用 uvloop（独立 `asyncio.run` 测试本就正常，印证是 uvloop 问题）。
2. **search 端点阻塞事件循环**：`api/app.py` 的 `search_documents` 在协程里**同步**调 `service.search()`，其内部 `RerankerService.rerank()` 同步加载 710M 的 `BAAI/bge-reranker-v2-m3`，阻塞事件循环使首次检索请求超时。修复：`await asyncio.to_thread(service.search, ...)` 把整个同步 search 丢到工作线程（与 async_engine 一致）。

验证：eee/kb-2/kb-3 三 collection 文档全部 READY（各 24 chunks），`/api/collections/{name}/search` 返回含 `rerank_score` 的结果，端到端打通。换模型/重装后需删 `rag/data/` 重建向量库（documents.db 不自动清理）。未提交，待人工确认。

## opencli 适配器测试 CodingHub RAG 知识库

opencli 已装于 `/Users/kirito/.npm-global/bin/opencli`（v1.8.0，环境健康，无需重装）。注意 `~/.opencli/clis/showtime/*`（knowledge/create-kb/tool-share-test）指向 **watcher 系统 9090 端口**（另一项目，已停），与 CodingHub 无关，**不要误用**。

为用 opencli 测 CodingHub 自己的 RAG 知识库（端口 8000），新建 `codinghub` 站点适配器（validate PASS）：
- `kb-collections` → GET /api/collections
- `kb-docs --collection X` → GET /api/collections/X/documents/status
- `kb-upload --collection X --file F` → POST /api/collections/X/documents/batch (multipart)
- `kb-search --collection X --query Q --top-k N --rerank` → POST /api/collections/X/search

实测全部通过：列出 5 collection、eee 文档 READY(24 chunks)、语义检索返回 `rerank_score`、上传测试文档后状态 READY 且可检索。适配器文件路径 `~/.opencli/clis/codinghub/*.js`，可用 `OPENCLI_RAG_BASE` 覆盖基址。未提交，待人工确认。
