# 2026-07-28

## 完成：经验知识归档（复用统一互动代码约定）

- 通过 `ingest_note` 归档了一条 lesson 型笔记：「新增评论/点赞/收藏功能须复用统一互动代码，禁止重复造轮子」，状态已 confirm 为 verified 知识。
- 笔记指向真实代码：UnifiedLikeService / UnifiedFavoriteService / UnifiedCommentService / UnifiedInteractionController(`/api/v1/interactions`) / TargetType 枚举；关联模块「统一互动与通知」。
- 已同步写入 MEMORY.md 项目核心规则第 5 条，作为稳定跨会话约定。

## 完成：CodingHub MCP（项目自带 20 工具）全功能验证

- 启动方式：后端 `cd backend && ./gradlew bootRun`（8082，默认 mysql profile，DB=ai_tool_square）；RAG `cd rag && python3 server.py --host 127.0.0.1 --port 8000`；前端已在线(5173)。MCP Server 随后端启动，暴露 `/mcp`（streamable-http）与 `/sse`，`/mcp/**`、`/sse/**` 在 SecurityConfig 为 **permitAll（无需鉴权）**；写类工具内部用传入的 username/password 调 `userService.login` 鉴权（admin/Cloud@1234 可用）。
- **发现 1（服务状态）**：会话开始时后端虽端口 LISTEN 但 HTTP 全部超时（curl/python 均 HTTP 000，仅 nc 通）→ 空闲约 3 天致 DB 连接池僵死。重启后端即恢复。RAG 当时未启动，需手动拉起。
- **发现 2（真实 BUG）**：`h3_coding_hub_kb_document_status` 单文档分支（传 docId）返回**单个文档对象**，但工具声明的输出 JSON schema 强制要求 `kbId`/`documents`/`totalCount`（那是"列表"分支结构）→ 触发 `output validation failed: 未找到所需属性 kbId/documents/totalCount`。列表分支（不传 docId）正常。位置 `backend/.../mcp/IaihubToolHandler.java` `handleKbDocumentStatus`（L612-630）。修复方向：单文档分支也包成 `KbDocumentStatusResponse` 形状，或放宽输出 schema。
- **发现 3**：`/actuator/health` 返回 500 仅是未暴露 actuator（当作静态资源 404 被全局异常处理器转 500），不代表服务异常；`/mcp` POST initialize 正常返回 200。
- 其余 19 个工具（含 tool_search/post_search/kb_list/tool_get/tool_files/tool_download/post_get/kb_search/RAG 真实检索/kb_get_config/kb_upload_document/kb_create/update/configure/delete/tool_create/post_create/tool_modify/tool_file_upload/tool_file_delete）均验证通过（读类真实返回数据，写类真实创建并清理）。
- 验证脚本用 Python `mcp` SDK 1.27.1 `streamablehttp_client` 连 `http://127.0.0.1:8082/mcp`；测试产生的临时工具/帖子/KB/RAG 集合已全部清理（无残留）。

## 完成：CodeWiki MCP 全功能回归测试（第二轮）

- 24 个工具中 23 个通过；`generate_docs`(legacy) 因内置 LLM 订阅过期不可用（不影响 IDE 流程）。
- **重要发现 1**：会话开始时 `repowiki` 又被清空（15:16 一次 `analyze_repo` 重新初始化清掉了 13 个模块文档），已从 `/tmp/repowiki-backup` 完整恢复（modules + overview + module_tree.json）。→ 结论：不只 `close_session`，`analyze_repo` 也可能清空已有文档，操作前必须备份。
- **重要发现 2**：`edit_doc_file` 已修复！str_replace/insert/undo 三命令全部正常（此前 UnboundLocalError）。MEMORY.md 中"edit_doc_file 损坏"条目已过时。
- **小 bug**：`write_doc_file` 注入 frontmatter 时结束 `---` 与正文 `#` 标题粘连（`---# 标题`）。
- **小 bug**：`query_cross_service` 路由提取有噪声（JSON 字段误判为 PUT 路由），monorepo 内 matched links=0。
- `query_wiki` 在索引未建时返回空（不报错）；`close_session` 后恢复正常。本次 `close_session` 未清空文档。
- 测试产物（3 笔记、2 源文档、1 entity 页、1 issue、.trash）已全部清理；终态 `lint_wiki` health_score 100、0 error。备份更新至 `/tmp/repowiki-backup-20260728`。

## 完成：CodeWiki 全量生成

- 按 codewiki 命令流程重生成了 CodingHub 的完整 LLM Wiki（因上次 `close_session` 清掉了 repowiki，本次从 `analyze_repo` 重跑）。
- 产物：`repowiki/wiki/` 下 13 个模块文档（工具广场/实时聊天/平台基础/用户与认证/知识库与RAG/MCP服务/留言反馈/论坛社区/微课视频/统一互动与通知/前端类型定义/前端服务层/前端应用）+ `overview.md`；`index.md` 已更新。
- `lint_wiki` 全量检查：`health_score 100`、`0 error`（仅 14 条 missing_aliases info）。
- 关键坑已沉淀到 `MEMORY.md`（Mermaid 多节点单行/花括号/中文子图引号、`overview.md` 链接须 `modules/` 前缀、`write_doc_file` 不覆盖/`edit_doc_file` 损坏、`close_session` 可能清空产物需先备份）。

## 结构分析：analyze_impact 对 Java 实体类的局限性

- 用 `analyze_impact(file_paths=['.../model/ToolLike.java'], direction='depended_by')` 删除影响评估时，工具**只返回类自身**（含 `@onCreate` 方法），漏报了真实依赖方。
- 根因：CodeWiki 调用图只追踪**方法调用边**，不追踪 Java 的**泛型/字段类型/import 引用**。JPA 实体（`ToolLike`）被 `ToolLikeRepository extends JpaRepository<ToolLike, Long>` 和测试 `@Mock ToolLikeRepository` 引用，但这些是类型引用而非调用，故不在图里。
- 正确做法：评估「删除某类」的影响时，**必须用 `search_content` 全仓库搜类名 + Repository 名核实**，不能只信 `analyze_impact` 结果。`UnifiedLikeService` 里的 `buildToolLikes` 方法名含子串 "ToolLike" 但并未引用该类型，属搜索假阳性，需人工辨别。
- 本案中 ToolLike 体系已是 `@Deprecated` 死代码，真实业务已迁到 `UnifiedLike` 体系。

## 影响评估：Video.commentCount 类型修改

- 用 analyze_impact + 全文检索交叉验证评估了 `Video.commentCount` 改类型的爆炸半径：低-中风险（~12 类，集中在微课+统一互动模块，depth≤2）。
- 受影响：`Video`（updateScore/incrementCommentCount/Lombok）、`UnifiedCommentService`(L288/L315 `Math.max` 拆箱)、`VideoService`(L396/L432)、`UnifiedLikeService`/`UnifiedFavoriteService`（映射+内部DTO）、`VideoResponse`/`VideoListItem` DTO、5 个测试类的 builder 字面量。
- 最大风险：DB `video.comment_count` 列迁移 —— Hibernate `ddl-auto: update` 不会变更已有列类型，INT→BIGINT 须手写 Flyway 迁移（MySQL `MODIFY` / PG `ALTER COLUMN TYPE`），双库都要覆盖。
- 再次验证：analyze_impact 漏报 Lombok getter/setter 引用（非方法调用边），实体字段级改动必须补 `search_content` 全仓搜索。

## 完成：Wiki 增量更新（无实质变更）

- 执行 codewiki 增量更新流程：备份 → `analyze_repo` → 评估 changes → `lint_wiki` → `close_session`。
- `changes.changed_files` 仅含 `.codebuddy/memory/` 与 `repowiki/` 自身元数据/文档（commit 15c85962），**零源码变更**，故无模块需要更新。
- 本次 `analyze_repo` **未清空**文档（13 模块 + overview 完好），与 15:16 那次行为不同——清空非必现，但备份策略仍必须保留。
- `lint_wiki(stale_refs, broken_links)`：0 issue，health_score 100。`close_session` 已重建索引。
- 注意：`analyze_repo` 的 changes 基于 git commit 差异，`repowiki/` 与 memory 文件自身的提交也会出现在 changed_files 里，评估影响时应过滤掉非源码路径。

## 完成：外部文档知识抽取（MCP 最佳实践）

- 对 `docs/MCP Server 开发最佳实践与踩坑记录.md` 执行了 codewiki 知识抽取全流程：`ingest_source`（name=mcp-server-best-practices）→ 创建 7 处产物 → lint → `close_session` 重建索引 → `query_wiki` 验证可检索。
- 产物：1 源摘要页 `wiki/sources/mcp-server-best-practices.md`；3 实体页 `wiki/entities/`（McpPromptProvider / McpResourceHandler / McpNotificationService）；3 概念页 `wiki/concepts/`（MCP控制通道与数据通道分离 / Streamable-HTTP传输模式 / MCP与SKILL能力对等）；并在 `modules/MCP服务.md` 末尾新增「知识笔记与外部文档」反链小节。
- 终态：`lint_wiki` health_score 100、0 issue；BM25 检索命中新页且 related 图谱双向连通。
- **工具坑（新发现）**：`edit_doc_file` 的参数是 `command`（enum: str_replace/insert/undo）而非 `operation`，且 `filename` 不能带目录前缀（`modules/X.md` 会报错，用 `page_type` 路由）。
- **lint 技巧**：`raw/sources/` 原始副本会触发 orphan_pages 警告，解法是在 wiki 源摘要页用相对 markdown 链接 `../../raw/sources/x.md` 指向它。

## 完成：架构审查与热点分析（依赖图）

- 用 CodeWiki MCP 完成全仓架构审查：2847 组件、去噪后 1257 条有效边（必须过滤 `frontend/dist/`、`.codebuddy/`、`repowiki/` 噪声，否则 high_impact 榜单被压缩产物污染）。
- **核心结论**：分层纪律良好（service→repository 103 边、controller→service 66 边单向主导，controller 层零被依赖，最深依赖链仅 3 跳）；唯一系统性问题是 **`mcp ↔ service` 循环依赖簇（31/36 边）**，根因是 mcp 包双重职责——`McpConnectionManager/SseEmitterEvent`（SSE 基础设施，被依赖 70）+ `IaihubToolHandler`（编排，依赖 service）。重构方案：拆 SSE 部分到 `infra/sse`。
- 热点 Top：`SseEmitterEvent.builder`(70)、`ApiResponse.success/error`(67/55，扁平扇入 depth=1 风险可控)、`VideoRepository.findByIdAndStatus`/`ToolRepository.findByIdAndStatusNormal`(21，软删除过滤入口)、`IaihubToolHandler.toJson/successResult/errorResult`(20+)。
- 次要边界渗漏：`service/video` 直连核心 `repository` 与 `repository/tag`。
- 工具坑：连续两次 `list_dependencies`/`analyze_impact` 调用会**覆盖同一个 workspace json 文件**（dependencies.json / impact_analysis.json），需串行调用并及时读取；`list_dependencies(module_level=true)` 返回 total_modules=0，模块聚合需自己按路径分组计算。
- 本次 `analyze_repo` 未清空 repowiki（备份在 /tmp/repowiki-backup）。

## 过程发现

- `analyze_repo` 后 `list_components`/`get_module_tree`/`get_processing_order` 均可从 SQLite 缓存命中（重跑快）。
- Mermaid 校验器偶发 503（服务宕机），非语法错；稳定后严格校验，多节点单行与 `{id}` 标签会真实报错。
