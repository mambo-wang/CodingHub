# 2026-07-30 工作记忆

## CodeWiki 增量更新 Wiki（无源码变更的 no-op 场景）
- 运行 `analyze_repo(repowiki)` 检测自上次全量生成（基线 commit 42c49740）以来的增量。结果 `changes: null`；`git diff 42c49740..HEAD -- backend/src frontend/src rag/ docs/` 仅 `docs/codewiki_mcp_test_report.md`（182 行，测试报告，非 Wiki 内容源）有改动，**源码目录零改动**，工作树也未改任何源文件。
- 结论：无源码变更 → 无需更新任何模块文档。但仍按流程收尾：`lint_wiki(checks=[stale_refs,broken_links,orphan_pages,no_outlinks,coverage])` → **0 issues / health_score=100**；随后 `close_session` 重建 BM25 索引与 wikilink 图（已 `query_wiki` 验证可检索）。
- 观察：本次 `analyze_repo` **未清空** repowiki 模块文档（13 篇 + overview.md 均完好）。与之前"analyze_repo 会清空文档"的记忆不同——可能因 `.meta/metadata.json` 缺失走全量重分析但不删文档，仍建议操作前 `cp -R repowiki /tmp/repowiki-backup` 备份以防万一。
- 增量判定依据：当 `analyze_repo` 返回 `changes:null` 且源码目录 git diff 为空时，即判定无更新；不要盲目重写模块，以免破坏已 health_score=100 的文档。

## CodeWiki 外部文档知识抽取（OpenSpec design.md）
- 抽取对象：openspec/changes/archive/ 下全部 32 个需求的 design.md。ingest_source 逐个导入（用带日期唯一 name 防重名）；2026-06-17-user-nickname-feature 与 06-13 内容相同被跳过 → 实际 31 篇入库。
- 产出（均落 repowiki/wiki/）：31 篇 wiki/sources 摘要 + 13 篇 wiki/entities + 10 篇 wiki/concepts + 1 篇 hub（openspec知识抽取总览.md）= 55 篇。
- frontmatter 正确姿势：用 write_doc_file 的 frontmatter_extra 传 title/origin/source_type/version/tags/aliases/source_refs，正文不写 frontmatter 块 → 避免旧记忆的 ---# 粘连 bug。
- 命名：实体/概念页 slug 用中文短名；源页 slug 用 openspec 目录名；正文 wikilink 用中文短名+别名均可解析（lint broken_links=0）。
- raw/sources/ 是 ingest_source 归档的原始 design.md 副本，lint 会报 orphan_pages（无入链），是导入流程固有现象非错误（0 error/0 broken_links）。
- 多跳检索：`query_wiki(hop=1)` 成功，新 OpenSpec 知识图谱桥接到既有代码 Wiki 模块（统一互动架构→统一互动与通知、RAG自适应分块→知识库与RAG），实现设计决策↔代码实现可追溯。

## CodeWiki analyze_impact 影响范围评估（model 类坑）
- 对 `model/User.java` 跑 `analyze_impact(direction=depended_by)`：图只返回 7 组件（depth1 仅 4 个实体 Tool/FeedbackMessage/Notification/Danmaku）。反向 depends_on 仅 Role/AccountStatus 枚举。high_risk=0。
- **坑**：CodeWiki 缓存图只捕获**模型层实体间字段引用边**（@ManyToOne 等），缺失 service/controller/config/mcp 层引用边 → 对 model 类做影响分析会严重低估爆炸半径。
- **补全手段**：`grep "import com.iaihub.toolbox.model.User;"` 命中 **47 个文件**（生产 25：UserRepository + 9 Service + 12 Controller + 3 Config + 1 MCP + 3 Model；测试 16）。User 是核心账户模型，改动属**高风险**（跨 forum/tool/feedback/notification/video/kb/chat/mcp 全域；JwtAuthenticationFilter 每请求查库读 role/status）。
- 结论：model 类影响评估 = analyze_impact（图谱，看实体关系）+ grep import（看全量调用方），二者互补，不可只信图谱。
