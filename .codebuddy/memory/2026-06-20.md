# 2026-06-20 工作日志

## OpenSpec change: add-content-moderation（内容审核权限）

创建了 OpenSpec change `add-content-moderation`，为工具/帖子/微课引入管理员内容审核权限。7 个 artifacts 全部完成，位于 `openspec/changes/add-content-moderation/`。

### 核心决策
- **权限对称**：能删就能改。`canModify = isOwner || isAdmin`（ADMIN/SUPER_ADMIN 可删改任意内容）
- **后端签名变更**：三个 Service（ToolService/ForumPostService/VideoService）的 `deleteXxx`/`updateXxx` 方法签名从 `Long userId` 改为 `User user`，以便读取角色
- **MCP Handler 需适配**：`IaihubToolHandler.handleToolModify`（IaihubToolHandler.java:323）调用 `ToolService.updateTool`，签名变更需同步修改（MCP 层面权限不变，仍仅创建者）
- **前端 composable**：新增 `useContentPermissions(ownerId)` 统一计算 canEdit/canDelete
- **列表页按钮**：半透明(opacity 0.35)→hover高亮(opacity 1)，移动端可见
- **补齐编辑页**：帖子编辑页 PostEditorPage 接通编辑模式 + 新增微课编辑页 VideoEditPage

### 影响范围
- 后端：3 Service + 3 Controller + 1 MCP Handler
- 前端：1 composable + 3 卡片组件 + 3 列表页 + 3 详情页 + 2 编辑页 + 路由
- 测试：更新 ToolServiceTest，新增 ForumPostServiceTest、VideoServiceTest
- 风险等级 L1（签名变更，无 schema 变更）

### 待实现
运行 `/opsx:apply` 开始实现，共 10 组 33 个任务。
