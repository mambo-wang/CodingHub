# 2026-07-29

## codebase-memory-mcp 安装与配置
- 通过 `npm install -g codebase-memory-mcp` 安装（v0.9.0），二进制位于 `/Users/kirito/.npm-global/bin/codebase-memory-mcp`。
- 已加入项目 `.mcp.json` 的 `mcpServers.codebase-memory`（type: stdio，无 `serve` 参数——该二进制没有 serve 子命令，直接运行即 MCP stdio 模式）。
- 提供 15 个工具：`index_repository` / `search_graph` / `query_graph` / `trace_path` / `get_code_snippet` / `get_graph_schema` / `get_architecture` / `search_code` / `list_projects` / `delete_project` / `index_status` / `detect_changes` / `manage_adr` / `ingest_traces`。
- cbm-wiki-generator skill 里的配置示例 `args:["serve"]` 是过时的，实际不需要。
- 注意：IDE 需重启/重载才能加载新的 MCP 服务器。

## 安装 QoderAI/better-harness（AI 编码工作流审查 harness）
- 仓库 `https://github.com/QoderAI/better-harness.git` 已安装到本项目。Shell 直连 GitHub 超时（curl 443 timeout），改用 `gh repo clone` 成功克隆（gh 走独立网络通道）。
- 安装布局：完整仓库置于 `.codebuddy/better-harness/`（repo root，含 scripts/references/templates/models）；技能通过相对符号链接 `.codebuddy/skills/better-harness -> ../better-harness/skills/better-harness` 暴露给 CodeBuddy 发现。
  - 选相对符号链接的原因：SKILL.md 用 `../..`（相对技能文件）定位 `<better-harness-root>`（须为含 scripts/ 的 repo root），CLI 自身用 `realpathSync` 解析。相对链接使「逻辑路径 `../..`」与「物理 realpath」都落到 `.codebuddy/better-harness`，路径链自洽。
  - CodeBuddy 技能为**非递归**发现（86 个 SKILL.md 全在 `.codebuddy/skills/<name>/SKILL.md` 一层深），故必须有深度为 1 的入口（此处为符号链接）。
- 运行：CLI 为 `node .codebuddy/better-harness/scripts/better-harness.mjs`（无需 `npm install`，核心路径不依赖 tree-sitter/esbuild）。注意 repo `engines` 要求 node `>=22.20 <25.0.0`，本机为 v25.8.1（略超范围，实测 help 正常）。
- 技能描述为 "Invoke only via slash command"，CodeBuddy 可能未注册 `/better-harness` 斜杠命令；可在会话重载后通过技能系统调用，或直接跑 node CLI。

## CodeWiki Wiki 全量重建（repowiki/ 此前已被删除）
- 用 CodeWiki MCP 从零重建 LLM Wiki：`analyze_repo`（1213 组件，session 重建）→ Python 分类脚本生成 10 模块 module_tree（用户与认证/工具广场/论坛社区/微课视频/知识库与RAG/实时聊天/统一互动与通知/MCP服务/平台基础/前端应用）→ `save_module_tree` → 10× `write_doc_file`（每篇 120-175 行，含 Constraint Index、Mermaid 架构图+时序图、带 confidence+Evidence 的业务约束）→ `overview.md` 直接 write_to_file（非法 page_type）→ `lint_wiki`（0 error/38 warning 均为方法级 undocumented，不阻断）→ `close_session` → `query_wiki` 验证 BM25 检索与 wikilink 双向图正常。
- 组件情报来源：10 个 code-explorer 子代理并行探究各域（端点表、业务规则+源码证据、数据模型、依赖）。
- 本次发现并写入 Wiki 的代码问题：① 前端 `ChatLauncher.vue` 引用 `stores/chat.ts` 不存在的 `drawerOpen/unreadCount/openDrawer/closeDrawer`（重构遗留）；② `forum.ts` 独立 axios 实例无 401 刷新拦截器；③ `POST /api/v1/tools/{toolId}/files` permitAll 且 userId 可伪造；④ Flyway 未启用，schema 全靠 ddl-auto:update（迁移脚本仅参考）；⑤ 排行榜 OverviewServiceImpl 全表 findAll 内存排序。
- 新坑：`query_wiki` 现在要求显式传 `output_dir`（如 /Users/kirito/repos/CodingHub/repowiki），否则报 "output_dir is required"。

## 运行 better-harness 审查（/better-harness 在本项目落地）
- 本次成功跑通完整工作流：`harness evidence-bundle`（platform=qoder, depth=normal, zh）→ 三路证据 → 主导对账 → `harness render --mode html --validate`（两轮校验均 pass）。
- **关键限制**：本环境 `Task` 工具不可用，无法并行 3 个 subagent；改为 lead 直接读证据包三路 + 实际工程面（Makefile/AGENTS.md/.codebuddy/{rules,commands}）做对账。
- **会话遥测缺失**：evidence-bundle 的 sessionEvidence 为 0 候选（`eligibleCount:0, analyzedCount:0, evidenceMode:"session-limited"`），因 CodeBuddy 无 Qoder 会话存储。故 change-validation/reliable-delivery 的行为性检查只能标 `Unobserved`，绝不臆造。
- **agent-customize 只扫了 `.qoder/*`**（36 skills/13 commands/0 hooks，integrity 0 问题），未覆盖运行期 `.codebuddy/skills`（86+）。渲染时 `summary.dimensions[].label` 必须用 canonical 中文（任务理解/可控执行/改动验证/可靠交付/经验沉淀），否则 INVALID_FINDINGS。
- 产出 HTML：`/Users/kirito/repos/CodingHub/.codebuddy/better-harness/runs/2026-07-29/report/2026-07-29/113006-CodingHub/report.html`。4 条 findings（2 Medium: 无自动校验门禁、无会话遥测；2 Low: 资产根错位、技能过长）。维度评分：任务理解80/可控执行85/改动验证55/可靠交付50/经验沉淀60。
- 结论：控制面「配置齐全但缺自动强制门禁与行为遥测」，属 needs-improvement。fix 提示已写入 findings，未自动改代码（遵循禁止私自提交）。

## CodeWiki Wiki 再次全量重建（12 模块版，覆盖上午的 10 模块版）
- repowiki/ 会话前不存在（上午版本又被清），从零重建：`analyze_repo`（1213 组件：Java 860/TS 167/Py 146/JS 40）→ classify.py 按目录/领域规则聚类 **12 个叶模块**（较上午新增 留言反馈、标签系统 独立成模块；「前端应用」改名「前端基础」）→ `save_module_tree` → 12× `write_doc_file`（87-141 行/篇，每篇 2-3 个 Mermaid 图）→ overview.md 直接 write_to_file（122 行）→ `lint_wiki` **health_score 100 / 0 issues** → `close_session` → `query_wiki` BM25 检索验证通过。
- 12 模块：平台基础/用户与认证/前端基础/工具广场/论坛社区/微课视频/知识库与RAG/留言反馈/统一互动与通知/标签系统/实时聊天/MCP服务。
- 本次情报来源：`read_code_components(include_bodies=false)` 读控制器/服务/模型签名 + grep 提取 Mapping/字段，未用子代理，成本远低于上午方案。
- 新坑：`write_doc_file` 内容较长时 MCP 调用可能 "terminated" 中断（微课视频篇首次失败），文件未落盘，重试即可；重试前先 ls 确认未创建（write_doc_file 不覆盖已存在文件）。
- 聚类脚本与 mod_*.json 临时文件已清理；备份习惯保持（cp -R repowiki /tmp/repowiki-backup）。

## CodeWiki MCP 全量功能验证（按 7 个 Prompt 工作流维度）
- 逐个实际调用 22 个工具（含 2 legacy），报告落盘 `docs/codewiki_mcp_test_report.md`。
- 🔴 两个 legacy 工具**服务端签名 bug**：`get_module_tree` 与 `generate_docs` 均报 `takes 1 positional argument but 2 were given`，完全不可用（IDE 细粒度流程不依赖它们）。
- 🟡 `query_cross_service` 在 `analyze_workspace` 后**返回空**：根因是 analyze_workspace 仅在响应里给出跨服务链接，未持久化 `workspace_routes.json` 之类索引。→ 跨服务追踪工作流（3.7）无法端到端闭环（在 /tmp 隔离双仓库实测：analyze_workspace 检测 1 链接成功，query_cross_service 取回 0）。
- 🟡 `save_module_tree` **忽略 `output_dir`**，强制写入仓库 `repowiki/.meta/module_tree.json`（覆盖既有树）。调用前务必备份 module_tree。
- 🟡 `query_wiki` 事实**必填 `output_dir`**（schema 标可选，服务端强校验）。
- 枚举差异（schema/示例与实测）：`analyze_impact.direction` 实为 `depended_by/depends_on/both`（非 callers/callees）；`flag_issue.severity` 实为 `error/warning/info`（非 low/medium/high）；`lint_wiki.checks` 实际跑 13 项（含文档未列的 overview_stale/unsupported_claims/isolated_components），用 `all` 最稳。
- ✅ 正常：analyze_repo/list_components/list_dependencies/analyze_impact/read_code_components/view_repo_file/write_doc_file/edit_doc_file/get_processing_order/get_prompt/ingest_note/ingest_source/retract_source/batch_ingest/lint_wiki/flag_issue/analyze_workspace/close_session。
- 安全：验证前已 `cp -R repowiki /tmp/repowiki-backup-<date>`，写入型调用均在可恢复范围；`analyze_workspace` 用 `/tmp` 隔离双仓库；验证后已用备份完整覆盖 repowiki 并清理 /tmp，query_wiki 回归正常。

## CodeWiki MCP 二次回归验证（5 个问题项已全部修复）
- 用户修复后复测：原 🔴🔴 `get_module_tree`/`generate_docs` 签名 bug（takes 1 positional argument but 2 were given）已消除——前者正常返回 12 模块树，后者优雅返回 InvalidSubscription（无 CodingPlan 订阅，预期）。原 🟡🟡🟡 三项偏差已修复：
  - `query_cross_service` 现在能取回数据：`analyze_workspace` 已落盘 `workspace_routes.json`+`cross_service_links.json`，`query_cross_service` 返回 1 条 repo-a→repo-b 链接。**坑**：两工具 `output_dir` 须一致（或均用默认 `<workspace>/workspace-wiki`），否则仍返回空。
  - `save_module_tree` 现尊重 `output_dir`，不再越权写仓库 repowiki。
  - `query_wiki` 现接受 `repo_path` 推导 `output_dir`，可不传 `output_dir`。
- 报告 `docs/codewiki_mcp_test_report.md` 已追加「4.5 二次回归验证明细」并更新汇总表/工作流结论：现有 22 工具全部通过，7 个工作流端到端可用。
- repowiki 验证后完好（12 模块），/tmp 临时产物与备份已清理。

## 增量更新 Wiki 命令执行（2026-07-29）
- 跑 `analyze_repo`：因 `repowiki/.meta/metadata.json` 不存在 → 走**全量分析**（changes=null，无基线可 diff）。
- 关键核查：源码侧 `backend/src`、`frontend/src`、`rag/` 相对 HEAD **零改动**（git diff 全空）→ 无源码变更 → **无模块需要增量更新**。
- `lint_wiki(checks=all)`：**health_score=100，13 项检查 0 问题**。磁盘 12 个模块文档均在、自洽、无断链/过时引用。
- 发现：工作区 repowiki 相对 git HEAD 有未提交删除（concepts/、entities/、raw/、notes/ 全空，.meta 缺 edit_history/issues/source_registry/metadata）。这些是之前测试 session 中 `close_session`/`analyze_repo` 清空文档留下的副作用；因现有模块文档未引用它们（lint 不报 broken_links/stale_refs），恢复反而可能引入不一致 → **不恢复**。
- `query_wiki` 验证 BM25 检索正常（返回 5 条相关），索引已随 analyze_repo 重建。
- 决策：跳过命令 Step5 的 `close_session`（有清空文档风险，且索引已新鲜、无变更需重建）；未完成任何写入，repowiki 状态未恶化。结论：源码未变、wiki 健康满分，本次增量更新无可做项。
- 提醒（待用户决定）：提交时若 `git add repowiki/` 会把这些辅助页删除带入提交；可按需选择恢复或接受删除。

## CodeWiki Wiki 第三次全量重建（13 模块版，本次为最新有效版本）
- repowiki/ 会话前不存在（又被清空），从零重建。流程：`analyze_repo`（1213 组件）→ classify.py 聚类 **13 个叶模块**（1099 组件、0 未分类）→ `save_module_tree` → `get_processing_order` → 13× `write_doc_file`（66-163 行/篇，每篇 1-3 个 Mermaid 图，全部语法校验通过）→ overview.md 直接 write_to_file（154 行）→ `lint_wiki` **health_score=100 / 0 issues** → `close_session` → `query_wiki` BM25 检索验证通过。
- 13 模块（较 12 模块版变化）：平台基础/用户与认证/工具广场/论坛社区/微课视频/知识库与RAG/留言反馈/统一互动与通知/实时聊天/MCP服务 + 前端拆三层：**前端类型定义(L0)/前端服务层(L1)/前端应用(L2-L4)**；标签系统并回「统一互动与通知」。
- 情报来源：每模块 read_file 精读 2-4 个关键源文件（Controller/Model/Config）+ AGENTS.md 既有知识，未用子代理，效率最高的一版。
- 文档正文经 /tmp/doc_*.md 中转（write_doc_file 用 content_file 参数传入），避免长内容 MCP 调用中断；临时文件已清理。