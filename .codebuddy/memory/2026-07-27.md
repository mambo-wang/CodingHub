# 2026-07-27

## 重新生成 CodeWiki 仓库 Wiki（repowiki/）

- 使用 codewiki MCP 完整工作流重建 Wiki：analyze_repo（session `80927d3ce501`，2847 组件）→ 聚类 → 逐模块撰写 → overview → lint → close_session。
- 模块划分为 **10 个叶模块**（较旧版 7 个新增：实时聊天、微课视频、平台基础，并将互动+通知+反馈合并为「统一互动与通知」）：MCP服务、知识库与RAG、论坛社区、微课视频、实时聊天、统一互动与通知、用户与认证、工具广场、平台基础、前端应用。
- 产出：`repowiki/wiki/overview.md`（144 行）+ `repowiki/wiki/modules/*.md`（10 篇，111~172 行/篇，每篇含 Mermaid 图与交叉引用）。lint 结果 0 error；208 warning 全部为 `frontend/dist` 构建产物覆盖率噪音。
- 经验：
  - `analyze_repo` 首次运行会 MCP 超时（>60s），但后台已完成；session_id 可从 `.codewiki/sessions/<id>/summary.json` 恢复。
  - 组件清单可从 `.codewiki/analysis_cache.db`（SQLite，components 表）导出用于聚类。
  - 聚类正则注意 `toolbox` 包名会误匹配 `tool`，需用锚定/路径段匹配。
  - `write_doc_file` 的 page_type 无 `overview` 枚举，总览需直接写 `repowiki/wiki/overview.md`。
  - module_tree 需剔除 `frontend/dist` 构建产物组件。
- 当前平台能力快照（写入文档）：MCP 20 tools（8 工具+3 论坛+9 知识库）/3 resources/6 prompts，Streamable HTTP + SSE；新增实时聊天（STOMP /ws，消息/回应/编辑/撤回/typing/在线数）；Flyway 迁移至 V11（chat_room_p1）。

## OpenSpec 设计文档知识抽取（外部文档知识抽取工作流）

- 从 `openspec/changes/archive/` 导入 31 个 design.md 到 `repowiki/raw/sources/`（1 个重复跳过）。
- 生成知识页面共 21 篇：
  - **source** (1): `wiki/sources/openspec-design-archive.md` — 31 个变更的总览表
  - **entity** (12): ForumPost, Tool, Video, User, KnowledgeBase, Tag, Notification, ChatMessage, FeedbackMessage, McpServer, RagService, ToolFile
  - **concept** (8): jwt-dual-token-auth, unified-interaction, soft-delete, rbac-permission, hotness-scoring, http-range-streaming, rag-adaptive-chunking, dual-database, async-batch-upload, content-moderation, notification-system
- 所有页面通过 `[[wikilink]]` 互联，query_wiki 多跳搜索验证通过（hop=2 可达全部相关页面）。
- 经验：`ingest_source` 参数名是 `source_path`（非 file_path）；`write_doc_file` 用 `output_dir` 替代 `session_id` 可无会话写入。

## Tool.java 修改影响范围评估

- 使用 `analyze_repo`（session `5c990f2176b0`，2847 组件）+ 手动代码搜索完成影响分析（MCP `analyze_impact` 工具在分析完成后断连，改用 `search_content` 逐层追踪 import 链）。
- **爆炸半径**：~58 个受影响组件（后端主代码 15 + 测试 8 + DTO 13 + 前端 22），🔴 高风险。
- **模块扩散**：跨 7 层（model → repository → service → controller → mcp → dto → frontend）。
- **高风险组件**：`ToolRepository`（7 个直接依赖者）、`ToolService`（3 个）。
- **入口点**：5 个 REST 控制器（ToolController, ToolFileController, UnifiedInteractionController, UserController, OverviewController）+ MCP 18 tools + 13 个前端页面/组件。
- **Tool.java 自身依赖**：仅 `Category`（@ManyToOne）和 `User`（@ManyToOne），反向依赖面窄。
- 经验：CodeWiki MCP 的 `analyze_impact` 等工具可能在 `analyze_repo` 完成后断连，需准备手动 import 链追踪作为降级方案。
