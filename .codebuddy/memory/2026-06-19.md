# 2026-06-19 工作日志

## OpenSpec schema 增加后端单元测试要求

- 修改 `openspec/schemas/sdd-with-ui-design-tdd-task/schema.yaml`：
  - `tasks` artifact 的 `instruction` 增加「单元测试要求（强制）」章节
  - 要求后端代码必须编写单元测试并保证通过（不强制 TDD 流程）
  - 测试用例来源于 spec.md 的 Scenario，框架 JUnit5 + Mockito
  - 运行命令 `cd backend && ./gradlew test`，测试失败不得标记完成
  - 前端代码不强制单元测试
- 修改 `templates/tasks.md`：每个任务分组增加后端单元测试占位项
- YAML 格式已验证通过

## 创建 OpenSpec change: add-user-role-approval

- 变更名称: `add-user-role-approval`
- 路径: `openspec/changes/add-user-role-approval/`
- Schema: `sdd-with-ui-design-unittest`
- 7/7 artifacts 全部完成: proposal, design, design-system, impact-analysis, specs, ui-preview, tasks
- 核心设计决策:
  - 三级单角色: USER / ADMIN / SUPER_ADMIN（User 表加枚举字段）
  - 账号状态: ACTIVE / PENDING / REJECTED / DISABLED
  - 注册只暴露 USER / ADMIN，SUPER_ADMIN 不可注册
  - USER 注册直接 ACTIVE，ADMIN 注册 PENDING 待审批
  - 超管内置账号 admin / Cloud@1234，DataInitializer 启动时初始化
  - JWT 不放 role，filter 查库读 role/status 构建 GrantedAuthority
  - REJECTED 用户 username 占用，不可重新注册
- impact-analysis 发现的关键修正: 前端 api.ts 需区分 401/403；导航栏组件实际为 AppHeader.vue
- tasks.md 共 11 组 33 个任务，含后端单元测试要求
